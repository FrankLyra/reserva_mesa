# Frontend Reserva de Mesas

App Angular 17 com o componente `MesaSelecaoComponent`.

```bash
npm install
npm start
```

O backend deve estar em `http://localhost:8081`.

Tela de login:

```text
http://localhost:4200/login
```
