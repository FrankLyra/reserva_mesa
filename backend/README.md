# Backend Reserva de Mesas

API REST Spring Boot para eventos, mesas e reservas com PIX mockado.

```bash
mvn spring-boot:run
```

Por padrao usa H2 em memoria. Para PostgreSQL, configure as variaveis do arquivo `.env.example`.
